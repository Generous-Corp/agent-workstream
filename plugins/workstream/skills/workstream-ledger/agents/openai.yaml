interface:
  display_name: "Workstream Ledger"
  short_description: "Preserve evolving tasks and decisions across agent sessions"
  default_prompt: "Run the bundled default bounded resume helper first, then restore or update this workstream from its validated current view and live PR state."
